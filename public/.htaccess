# Deny access to dotfiles (defense in depth — .env itself is not deployed inside
# this folder, but this blocks stray .env/.git/etc. if ever misplaced here).
<FilesMatch "^\.">
  Require all denied
</FilesMatch>

# No directory listings.
Options -Indexes

AddDefaultCharset utf-8
